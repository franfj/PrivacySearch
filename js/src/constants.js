// The root of the WordNet graph (entity)
var WORDNET_ROOT = "00001740";

// Set limitation in the wsd permutations step for efficiency purposes
var LIMITED_PERMUTATIONS = true;
var LIMITATION_LENGTH = 2;

// Set limitation in wsd score step for efficiency purposes
var MAX_PARENTS = 15;

// Number of characters that a WordNet term index has
var INDEX_LENGTH = 8;

// Index in which the term is in the data maps
var WORDNET_DATA_TERM_INDEX = 0;

